<Window x:Class="WarehouseInventoryTracker.AddProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseInventoryTracker"
        mc:Ignorable="d"
        Title="Add Product" Height="290" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Product ID:"/>
        <TextBox x:Name="ProductIdTextBox" Grid.Row="1" Margin="0,0,0,10"/>

        <Label Grid.Row="2" Content="Product Name:"/>
        <TextBox x:Name="ProductNameTextBox" Grid.Row="3" Margin="0,0,0,10"/>

        <Label Grid.Row="4" Content="Initial Quantity:"/>
        <TextBox x:Name="InitialQuantityTextBox" Grid.Row="5" Margin="0,0,0,10"/>

        <Label Grid.Row="6" Content="Reorder Threshold:"/>
        <TextBox x:Name="ReorderThresholdTextBox" Grid.Row="7" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>